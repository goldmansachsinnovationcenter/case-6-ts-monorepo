{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "echo no lint",
    "typecheck": "echo no typecheck"
  },
  "files": [
    "base.json",
    "react-library.json"
  ],
  "devDependencies": {
    "vitest": "^3.1.3"
  }
}
